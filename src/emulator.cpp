#include "emulator.h"
#include <fstream>

void Emulator::run_file(std::ifstream file) {
    std::string line;
    for (std::string line; std::getline(file, line);) {
        
    }
}

void Emulator::process_line(std::string line) {
    // ree
}